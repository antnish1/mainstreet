import { SUPABASE_ANON_KEY, SUPABASE_URL } from './supabase-config.js';

const $=s=>document.querySelector(s);
const key='mainstreet.staff-session.v3';
const session=()=>JSON.parse(localStorage.getItem(key)||'null');
const token=()=>session()?.token||null;
let resolvedRole=String(window.mainstreetRole||document.body.dataset.role||session()?.user_role||session()?.role||'').toLowerCase();
const isAdmin=()=>resolvedRole==='admin'||String(window.mainstreetRole||document.body.dataset.role||session()?.user_role||session()?.role||'').toLowerCase()==='admin';
const headers={apikey:SUPABASE_ANON_KEY,Authorization:`Bearer ${SUPABASE_ANON_KEY}`,'Content-Type':'application/json'};
const money=n=>`₹${Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const safe=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const iso=d=>{const x=new Date(d);x.setMinutes(x.getMinutes()-x.getTimezoneOffset());return x.toISOString().slice(0,10)};
const today=()=>iso(new Date());
const state={data:null,section:'overview',preset:'today',loading:false};

async function rpc(name,body){const r=await fetch(`${SUPABASE_URL}/rest/v1/rpc/${name}`,{method:'POST',headers,body:JSON.stringify(body)});const text=await r.text();let data;try{data=text?JSON.parse(text):null}catch{data=text}if(!r.ok)throw Error(data?.message||data?.hint||`Request failed (${r.status})`);return data}
function toast(message){const host=$('#toastRegion');if(!host)return;const d=document.createElement('div');d.className='toast';d.textContent=message;host.append(d);setTimeout(()=>d.remove(),3200)}

function inject(){const view=$('#reportView');if(!view||$('#cafeReports'))return;view.classList.add('cafe-report-host');const shell=document.createElement('section');shell.id='cafeReports';shell.className='cafe-reports';shell.innerHTML=`
  <header class="cafe-report-head">
    <div class="cafe-report-title"><h2>Café Reports</h2><small id="cafeReportPeriod"></small></div>
    <div class="cafe-report-head-actions"><button id="cafeReportExport" type="button" aria-label="Export report" title="Export">⇩</button><button id="cafeReportRefresh" type="button" aria-label="Refresh report" title="Refresh">↻</button></div>
  </header>
  <nav id="cafeReportTools" class="cafe-report-tools" aria-label="Administrative report tools">
    <button id="cafeOpeningBalance" class="admin-report-tool" type="button"><span>＋</span>Opening balance</button>
    <button id="cafeTodayEntries" class="admin-report-tool" type="button"><span>✎</span>Edit today’s entries</button>
  </nav>
  <nav id="cafePeriodPresets" class="cafe-period-presets">
    <button data-preset="today" class="active">Today</button><button data-preset="yesterday">Yesterday</button><button data-preset="7days">7 Days</button><button data-preset="month">Month</button><button data-preset="custom">Custom</button>
  </nav>
  <section id="cafeCustomDates" class="cafe-custom-dates" hidden><label>From<input id="cafeReportFrom" type="date"></label><label>To<input id="cafeReportTo" type="date"></label><button id="cafeApplyDates" type="button">Apply</button></section>
  <nav id="cafeReportTabs" class="cafe-report-tabs">
    <button data-section="overview" class="active">Overview</button><button data-section="sales">Sales</button><button data-section="collections">Collections</button><button data-section="expenses">Expenses</button><button data-section="customers">Customers</button><button data-section="items">Items</button><button data-section="activity">Activity</button>
  </nav>
  <main id="cafeReportContent" class="cafe-report-content"><div class="cafe-report-loading">Loading business report…</div></main>`;
  view.append(shell);bind();syncToolPermissions();setPreset('today');
}

function bind(){
  $('#cafePeriodPresets').onclick=e=>{const b=e.target.closest('[data-preset]');if(b)setPreset(b.dataset.preset)};
  $('#cafeReportTabs').onclick=e=>{const b=e.target.closest('[data-section]');if(!b)return;selectSection(b.dataset.section)};
  $('#cafeReportRefresh').onclick=load;
  $('#cafeApplyDates').onclick=load;
  $('#cafeReportExport').onclick=exportReport;
  $('#cafeOpeningBalance').onclick=openOpeningBalance;
  $('#cafeTodayEntries').onclick=()=>{setPreset('today');selectSection('activity')};
  $('#cafeReportContent').onclick=handleContentClick;
  $('#editDialog')?.addEventListener('close',()=>{if(state.section==='activity')setTimeout(load,80)});
  document.addEventListener('mainstreet-role-ready',event=>{resolvedRole=String(event.detail?.role||window.mainstreetRole||document.body.dataset.role||session()?.user_role||session()?.role||'').toLowerCase();syncToolPermissions();render()});
  document.querySelector('.tab[data-view="report"]')?.addEventListener('click',()=>setTimeout(load,30));
}
function syncToolPermissions(){document.querySelectorAll('.admin-report-tool').forEach(button=>button.hidden=!isAdmin())}
function selectSection(section){state.section=section;document.querySelectorAll('#cafeReportTabs button').forEach(x=>x.classList.toggle('active',x.dataset.section===section));render()}
function openOpeningBalance(){if(!isAdmin())return toast('Administrator access required');$('#openLedger')?.click();let attempts=0;const timer=setInterval(()=>{attempts++;const button=$('#openingBalanceButton');if(button){clearInterval(timer);button.click()}else if(attempts>12){clearInterval(timer);toast('Open Customer Ledger and tap Opening balance')}},80)}
function handleContentClick(event){const edit=event.target.closest('[data-report-edit]');if(!edit)return;if(!isAdmin())return toast('Administrator access required');$('#editId').value=edit.dataset.id;$('#editType').value=edit.dataset.type;$('#editDate').value=edit.dataset.date;$('#editCustomer').value=edit.dataset.party;$('#editAmount').value=edit.dataset.amount;$('#editNote').value=edit.dataset.note||'';$('#editDialog')?.showModal()}

function setPreset(preset){state.preset=preset;document.querySelectorAll('#cafePeriodPresets button').forEach(b=>b.classList.toggle('active',b.dataset.preset===preset));const custom=$('#cafeCustomDates');custom.hidden=preset!=='custom';const end=new Date(),start=new Date();if(preset==='yesterday'){start.setDate(start.getDate()-1);end.setDate(end.getDate()-1)}else if(preset==='7days')start.setDate(start.getDate()-6);else if(preset==='month')start.setDate(1);$('#cafeReportFrom').value=iso(start);$('#cafeReportTo').value=iso(end);if(preset!=='custom')load()}

async function load(){if(state.loading||!token())return;const from=$('#cafeReportFrom').value,to=$('#cafeReportTo').value;if(!from||!to)return;state.loading=true;$('#cafeReportContent').innerHTML='<div class="cafe-report-loading">Calculating complete café report…</div>';try{state.data=await rpc('get_cafe_business_report',{p_token:token(),p_from:from,p_to:to});$('#cafeReportPeriod').textContent=`${formatDate(from)} — ${formatDate(to)}`;render()}catch(e){$('#cafeReportContent').innerHTML=`<div class="cafe-report-error"><b>Report unavailable</b><span>${safe(e.message)}</span></div>`}finally{state.loading=false}}
function formatDate(v){return new Date(`${v}T00:00:00`).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}
function kpi(label,value,tone=''){return `<article class="cafe-kpi ${tone}"><span>${safe(label)}</span><b>${money(value)}</b></article>`}
function metric(label,value){return `<div class="cafe-metric"><span>${safe(label)}</span><b>${typeof value==='number'?value.toLocaleString('en-IN'):safe(value)}</b></div>`}
function empty(text){return `<div class="cafe-report-empty">${safe(text)}</div>`}

function render(){if(!state.data)return;const renderers={overview:renderOverview,sales:renderSales,collections:renderCollections,expenses:renderExpenses,customers:renderCustomers,items:renderItems,activity:renderActivity};$('#cafeReportContent').innerHTML=(renderers[state.section]||renderOverview)()}
function renderOverview(){const o=state.data.overview||{},s=state.data.sales||{};return `<section class="cafe-primary-kpis">${kpi('Total sales',o.total_sales,'sales')}${kpi('Collections',o.total_collections,'collection')}${kpi('Expenses',o.expenses,'expense')}${kpi('Receivable',o.receivable,'receivable')}</section><section class="cafe-mini-strip">${metric('Cash',money(o.cash_received))}${metric('UPI',money(o.upi_received))}${metric('Bills',o.bill_count||0)}${metric('Avg bill',money(o.average_bill))}</section><section class="cafe-result-card"><div><span>Sales less recorded expenses</span><b>${money(o.sales_less_expenses)}</b><small>Operational indicator—not accounting net profit</small></div></section><section class="cafe-breakdown-card"><header><h3>Sales mix</h3></header><div class="cafe-breakdown-row"><span>Counter sales</span><b>${money(o.counter_sales)}</b></div><div class="cafe-breakdown-row"><span>Credit sales</span><b>${money(o.credit_sales)}</b></div><div class="cafe-breakdown-row"><span>Customer collections</span><b>${money(o.customer_collections)}</b></div><div class="cafe-breakdown-row"><span>Quick amount sales</span><b>${money(s.counter_quick_sales)}</b></div></section>`}
function renderSales(){const s=state.data.sales||{},rows=state.data.sales_activity||[];return `<section class="cafe-primary-kpis">${kpi('Total sales',(s.counter_item_sales||0)+(s.counter_quick_sales||0)+(s.counter_adjustments||0)+(s.credit_sales||0),'sales')}${kpi('Counter sales',(s.counter_item_sales||0)+(s.counter_quick_sales||0)+(s.counter_adjustments||0))}${kpi('Credit sales',s.credit_sales)}${kpi('Quick sales',s.counter_quick_sales)}</section><section class="cafe-mini-strip">${metric('Counter bills',s.counter_bills||0)}${metric('Credit bills',s.credit_bills||0)}${metric('Cash',money(s.cash))}${metric('UPI',money(s.upi))}</section><section class="cafe-list-card"><header><h3>Sales transactions</h3></header>${rows.length?rows.map(activityRow).join(''):empty('No sales in this period')}</section>`}
function renderCollections(){const c=state.data.collections||{};return `<section class="cafe-primary-kpis three">${kpi('Counter cash',c.counter_cash,'collection')}${kpi('Counter UPI',c.counter_upi,'collection')}${kpi('Customer payments',c.customer_collections,'collection')}</section><section class="cafe-notice"><b>Customer payment mode is not classified yet</b><span>Existing customer payments are included accurately in total collections, but Cash/UPI split is not guessed from free-text notes.</span></section><section class="cafe-result-card"><div><span>Total money received</span><b>${money((c.counter_cash||0)+(c.counter_upi||0)+(c.customer_collections||0))}</b><small>${Number(c.customer_collection_entries||0)} customer payment entries</small></div></section>`}
function renderExpenses(){const e=state.data.expenses||{},rows=e.by_category||[];return `<section class="cafe-primary-kpis two">${kpi('Total expenses',e.total,'expense')}${kpi('Entries',0,'count').replace(money(0),Number(e.entries||0).toLocaleString('en-IN'))}</section><section class="cafe-list-card"><header><h3>Expense categories</h3></header>${rows.length?rows.map(x=>`<div class="cafe-ranking-row"><div><b>${safe(x.category)}</b><small>${Number(x.entries||0)} entries</small></div><strong>${money(x.amount)}</strong></div>`).join(''):empty('No expenses in this period')}</section><button class="cafe-open-tool" type="button" onclick="document.querySelector('#openExpense')?.click()">Open expense ledger</button>`}
function renderCustomers(){const c=state.data.customers||{},rows=c.rows||[];return `<section class="cafe-primary-kpis three">${kpi('Receivable',c.receivable,'receivable')}${kpi('Debit customers',0).replace(money(0),Number(c.debit_customers||0).toLocaleString('en-IN'))}${kpi('Customers',0).replace(money(0),Number(c.total_customers||0).toLocaleString('en-IN'))}</section><button class="cafe-open-tool" type="button" onclick="document.querySelector('#openLedger')?.click()">Open customer ledger</button><section class="cafe-list-card"><header><h3>Customer balances</h3><small>Tap a customer to open ledger</small></header>${rows.length?rows.map(x=>`<button class="cafe-customer-row customer-summary-row" type="button" data-name="${safe(x.name)}" data-phone="${safe(x.phone)}"><div><b>${safe(x.name)}</b><small>${x.last_invoice?`Last invoice ${formatDate(x.last_invoice)}`:'No invoice'}${x.last_payment?` · Paid ${formatDate(x.last_payment)}`:''}</small></div><strong class="${Number(x.balance)>0?'debit':'credit'}">${money(x.balance)}</strong></button>`).join(''):empty('No customers found')}</section>`}
function renderItems(){const d=state.data.items||{},rows=d.rows||[],cats=d.categories||[];return `<section class="cafe-notice warning"><b>Unclassified quick sales: ${money(d.unclassified_quick_sales)}</b><span>Quick Amount sales have no item details and are intentionally excluded from item rankings.</span></section><section class="cafe-list-card"><header><h3>Top items</h3></header>${rows.length?rows.map((x,i)=>`<div class="cafe-ranking-row"><i>${i+1}</i><div><b>${safe(x.item_name)}</b><small>${safe(x.category)} · H ${Number(x.half_qty||0)} · F ${Number(x.full_qty||0)} · Qty ${Number(x.qty||0)}</small></div><strong>${money(x.sales)}</strong></div>`).join(''):empty('No item-based sales')}</section><section class="cafe-list-card"><header><h3>Category performance</h3></header>${cats.length?cats.map(x=>`<div class="cafe-ranking-row"><div><b>${safe(x.category)}</b><small>${Number(x.qty||0)} portions</small></div><strong>${money(x.sales)}</strong></div>`).join(''):empty('No category sales')}</section>`}
function renderActivity(){const rows=state.data.activity||[];return `<section class="cafe-list-card"><header><h3>Complete activity</h3><small>${isAdmin()?'Today’s invoices and payments can be edited':'Sales, collections and expenses'}</small></header>${rows.length?rows.map(activityRow).join(''):empty('No activity in this period')}</section>`}
function activityRow(x){const labels={counter_sale:'Counter sale',credit_invoice:'Credit invoice',payment:'Customer payment',expense:'Expense'};const editable=isAdmin()&&x.tx_date===today()&&(x.type==='credit_invoice'||x.type==='payment');const edit=editable?`<button class="cafe-activity-edit" type="button" data-report-edit data-id="${safe(x.id)}" data-type="${x.type==='credit_invoice'?'invoice':'payment'}" data-date="${safe(x.tx_date)}" data-party="${safe(x.party)}" data-amount="${Number(x.amount||0)}" data-note="${safe(x.detail||'')}">Edit</button>`:'';return `<div class="cafe-activity-row ${safe(x.type)}"><div><b>${safe(x.reference)}</b><small>${safe(labels[x.type]||x.type)}${x.party?` · ${safe(x.party)}`:''} · ${formatDate(x.tx_date)}</small></div><div class="cafe-activity-value"><strong>${x.type==='expense'?'-':''}${money(x.amount)}</strong>${edit}</div></div>`}

function exportReport(){if(!state.data)return toast('Load report first');if(!window.XLSX)return toast('Excel library is loading');const o=state.data.overview||{};const summary=Object.entries(o).map(([Metric,Value])=>({Metric:Metric.replaceAll('_',' '),Value}));const activity=(state.data.activity||[]).map(x=>({Date:x.tx_date,Type:x.type,Reference:x.reference,Party:x.party,Detail:x.detail,Amount:Number(x.amount)}));const items=(state.data.items?.rows||[]).map(x=>({Item:x.item_name,Category:x.category,HalfQty:x.half_qty,FullQty:x.full_qty,Quantity:x.qty,Sales:Number(x.sales)}));const customers=(state.data.customers?.rows||[]).map(x=>({Customer:x.name,Mobile:x.phone,Balance:Number(x.balance),LastInvoice:x.last_invoice,LastPayment:x.last_payment}));const book=XLSX.utils.book_new();[['Summary',summary],['Activity',activity],['Items',items],['Customers',customers]].forEach(([name,rows])=>XLSX.utils.book_append_sheet(book,XLSX.utils.json_to_sheet(rows),name));XLSX.writeFile(book,`Mainstreet-Cafe-Report-${$('#cafeReportFrom').value}-to-${$('#cafeReportTo').value}.xlsx`)}

if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject);else inject();